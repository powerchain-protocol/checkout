# PowerPay public stylesheet

The npm package publishes `powerpay.css` through:

```ts
import "@powerchain-protocol/powerpay-checkout-sdk/styles.css";
```

The explicit path is also exported:

```ts
import "@powerchain-protocol/powerpay-checkout-sdk/styles/powerpay.css";
```

The file is synchronized before builds and package creation.
