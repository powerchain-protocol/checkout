# Internal build reports

Build reports are retained for repository maintainers and release archives.

They are intentionally excluded from the npm package through the `files`
allowlist in `package.json`.
